<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.surit.user.mapper.UserMapper">
 
<insert id="insertUser" parameterType="UserDTO">
    INSERT INTO USERS (USER_ID, PASSWORD, NAME, PHONE, EMAIL, USER_ROLE)
    VALUES (#{userId}, #{password}, #{name}, #{phone}, #{email}, #{userRole})
</insert>
 
 <select id="countByUserId" parameterType="string" resultType="int">
 SELECT COUNT(*) FROM USERS WHERE USER_ID = #{userId}
 
 </select>
 
 <select id="selectByUserId" parameterType="string" resultType="UserDTO">
 SELECT USER_NO,USER_ID,EMAIL,
    PASSWORD, NAME,USER_ROLE,
    TO_CHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI') AS CREATED_AT_STR
    FROM USERS
    WHERE USER_ID = #{userId}
 
 </select>
 
 <delete id="deleteUser" parameterType="string">
 DELETE FROM USERS WHERE USER_ID = #{userId}
 </delete>
<insert id="insertAddress" parameterType="UserAddressDTO"
        useGeneratedKeys="true" keyProperty="addressId" keyColumn="ADDRESS_ID">
    INSERT INTO USER_ADDRESS (USER_NO, ZIP_CODE, ADDRESS, ADDRESS_DETAIL, IS_DEFAULT)
    VALUES (#{userNo}, #{zipCode}, #{address}, #{addressDetail}, #{isDefault})
</insert>
</mapper>

