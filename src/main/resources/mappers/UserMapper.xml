<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.surit.user.mapper.UserMapper">
 
 <insert id="insertUser" parameterType="UserDTO">
 INSERT INTO USERS (USER_ID, USER_PWD, USER_NAME, USER_EMAIL) 
 VALUES(#{userId}, #{userPwd}, #{userName}, #{userEmail})
 </insert>
 
 <select id="countByUserId" parameterType="string" resultType="int">
 SELECT COUNT(*) FROM USERS WHERE USER_ID = #{userId}
 
 </select>
 
 <select id="selectByUserID" parameterType="string" resultType="UserDTO">
 SELECT USER_ID,
    USER_PWD, USER_NAME, USER_EMAIL,
    TO_CHAR(USER_CREATE_AT, 'YYYY-MM-DD HH24:MI') AS CREATE_AT_STR
    FROM USERS
    WHERE USER_ID = #{userId}
 
 </select>
 
 <delete id="deleteUser" parameterType="string">
 DELETE FROM USERS WHERE USER_ID = #{userId}
 </delete>
  <insert id="insertAddress" parameterType="UserDTO">
 INSERT INTO USERS (
	    ADDRESS_ID,
	    ADDRESS_NAME,
        ZIPCODE,
        ADDRESS_BASIC,
        ADDRESS_DETAIL,
        IS_DEFAULT) 
 VALUES(#{addressId}, #{addressName}, #{zipcode}, #{addressBasic},#{addressDetail},#{isDefault})
 </insert>
</mapper>

