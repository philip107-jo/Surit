<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.surit.chat.mapper.ChatMapper">

	<!-- 공통 조각 : 방 1개를 읽을 때 쓰는 컬럼 -->
	<sql id="roomColumns">
		       R.ROOM_ID,
		       R.REQUEST_ID,
		       R.USER_NO,
		       R.FIXER_NO,
		       R.CATEGORY_CODE,
		       TO_CHAR(R.CREATED_AT, 'YYYY-MM-DD HH24:MI') AS CREATED_AT,
		       CU.NAME  AS USER_NAME,
		       FX.NAME  AS FIXER_NAME,
		       RQ.TITLE AS REQUEST_TITLE,
		       CC.CODE_NAME AS CATEGORY_NAME
	</sql>

	<sql id="roomJoins">
		  FROM CHAT_ROOM R
		  JOIN USERS CU                ON CU.USER_NO    = R.USER_NO
		  LEFT JOIN USERS FX           ON FX.USER_NO    = R.FIXER_NO
		  LEFT JOIN REPAIR_REQUESTS RQ ON RQ.REQUEST_ID = R.REQUEST_ID
		  LEFT JOIN COMMON_CODE CC     ON CC.CODE_ID    = R.CATEGORY_CODE
	</sql>

	<!-- ══════════ 채팅방 ══════════ -->

	<select id="selectRoomById" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT <include refid="roomColumns"/>
		<include refid="roomJoins"/>
		 WHERE R.ROOM_ID = #{roomId}
	</select>

	<select id="selectRoomByRequestId" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT * FROM (
			SELECT <include refid="roomColumns"/>
			<include refid="roomJoins"/>
			 WHERE R.REQUEST_ID = #{requestId}
			 ORDER BY R.ROOM_ID
		) WHERE ROWNUM = 1
	</select>

	<!-- 방 생성 전 재료 : 배정 기사는 채택된 견적으로 역추적 -->
	<select id="selectRoomSeedByRequest" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT RQ.REQUEST_ID,
		       RQ.USER_NO,
		       RQ.CATEGORY_CODE,
		       (SELECT E.FIXER_NO
		          FROM ESTIMATES E
		         WHERE E.REQUEST_ID = RQ.REQUEST_ID
		           AND E.STATUS = 'SELECTED'
		           AND ROWNUM = 1) AS FIXER_NO
		  FROM REPAIR_REQUESTS RQ
		 WHERE RQ.REQUEST_ID = #{requestId}
	</select>

	<!-- ROOM_ID 는 IDENTITY 라 넣지 않는다 (넣으면 ORA-32795) -->
	<insert id="insertRoom" parameterType="com.surit.chat.dto.ChatRoomDTO">
		INSERT INTO CHAT_ROOM (REQUEST_ID, USER_NO, FIXER_NO, CATEGORY_CODE, CREATED_AT)
		VALUES (#{requestId}, #{userNo}, #{fixerNo}, #{categoryCode}, SYSTIMESTAMP)
	</insert>

	<select id="selectRoomsByUserNo" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT <include refid="roomColumns"/>,
		       (SELECT M.CONTENT FROM (SELECT CONTENT FROM CHAT_MESSAGE
		          WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
		         WHERE ROWNUM = 1) AS LAST_MESSAGE,
		       (SELECT COUNT(*) FROM CHAT_MESSAGE M2
		         WHERE M2.ROOM_ID = R.ROOM_ID
		           AND M2.SENDER_NO != #{userNo}
		           AND M2.READ_AT IS NULL) AS UNREAD_COUNT
		<include refid="roomJoins"/>
		 WHERE R.USER_NO  = #{userNo}
		    OR R.FIXER_NO = #{userNo}
		 ORDER BY R.ROOM_ID DESC
	</select>

	<!-- ══════════ 문의방 (SUPPORT) ══════════ -->

	<select id="selectSupportUserNo" resultType="long">
		SELECT USER_NO FROM USERS WHERE USER_ID = 'surit_support'
	</select>

	<select id="selectInquiryTypes" resultType="com.surit.chat.dto.CodeDTO">
		SELECT CODE_ID, CODE_NAME
		  FROM COMMON_CODE
		 WHERE CODE_GROUP = 'INQUIRY_TYPE'
		   AND NVL(USE_YN, 'Y') = 'Y'
		 ORDER BY CODE_ID
	</select>

	<!-- 방금 만든 문의방 ID (문의방은 REQUEST_ID 가 없어서 이렇게 찾는다) -->
	<select id="selectLatestSupportRoomId" resultType="long">
		SELECT MAX(ROOM_ID) FROM CHAT_ROOM
		 WHERE USER_NO = #{userNo} AND REQUEST_ID IS NULL
	</select>

	<!-- 고객이 보는 내 문의 목록 -->
	<select id="selectMySupportRooms" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT R.ROOM_ID,
		       R.USER_NO,
		       R.CATEGORY_CODE,
		       CC.CODE_NAME AS CATEGORY_NAME,
		       TO_CHAR(R.CREATED_AT, 'YYYY-MM-DD HH24:MI') AS CREATED_AT,
		       (SELECT M.CONTENT FROM (SELECT CONTENT FROM CHAT_MESSAGE
		          WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
		         WHERE ROWNUM = 1) AS LAST_MESSAGE,
		       (SELECT M.SENT_AT FROM (SELECT TO_CHAR(SENT_AT,'YYYY-MM-DD HH24:MI') AS SENT_AT
		          FROM CHAT_MESSAGE WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
		         WHERE ROWNUM = 1) AS LAST_SENT_AT,
		       (SELECT COUNT(*) FROM CHAT_MESSAGE M2
		         WHERE M2.ROOM_ID = R.ROOM_ID
		           AND M2.SENDER_NO != #{userNo}
		           AND M2.READ_AT IS NULL) AS UNREAD_COUNT
		  FROM CHAT_ROOM R
		  LEFT JOIN COMMON_CODE CC ON CC.CODE_ID = R.CATEGORY_CODE
		 WHERE R.REQUEST_ID IS NULL
		   AND R.USER_NO = #{userNo}
		 ORDER BY R.ROOM_ID DESC
	</select>

	<!-- 관리자가 보는 전체 문의 목록 (filter: ALL / WAIT / DONE) -->
	<select id="selectAllSupportRooms" resultType="com.surit.chat.dto.ChatRoomDTO">
		SELECT * FROM (
			SELECT R.ROOM_ID,
			       R.USER_NO,
			       U.NAME AS USER_NAME,
			       R.CATEGORY_CODE,
			       CC.CODE_NAME AS CATEGORY_NAME,
			       TO_CHAR(R.CREATED_AT, 'YYYY-MM-DD HH24:MI') AS CREATED_AT,
			       (SELECT M.CONTENT FROM (SELECT CONTENT FROM CHAT_MESSAGE
			          WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
			         WHERE ROWNUM = 1) AS LAST_MESSAGE,
			       (SELECT M.SENDER_NO FROM (SELECT SENDER_NO FROM CHAT_MESSAGE
			          WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
			         WHERE ROWNUM = 1) AS LAST_SENDER_NO,
			       (SELECT M.SENT_AT FROM (SELECT TO_CHAR(SENT_AT,'YYYY-MM-DD HH24:MI') AS SENT_AT
			          FROM CHAT_MESSAGE WHERE ROOM_ID = R.ROOM_ID ORDER BY MESSAGE_ID DESC) M
			         WHERE ROWNUM = 1) AS LAST_SENT_AT,
			       (SELECT COUNT(*) FROM CHAT_MESSAGE M3
			         WHERE M3.ROOM_ID = R.ROOM_ID) AS MSG_COUNT
			  FROM CHAT_ROOM R
			  JOIN USERS U             ON U.USER_NO  = R.USER_NO
			  LEFT JOIN COMMON_CODE CC ON CC.CODE_ID = R.CATEGORY_CODE
			 WHERE R.REQUEST_ID IS NULL
		)
		<where>
			<if test="filter != null and filter == 'WAIT'">
				(LAST_SENDER_NO IS NULL OR LAST_SENDER_NO != #{supportNo})
			</if>
			<if test="filter != null and filter == 'DONE'">
				LAST_SENDER_NO = #{supportNo}
			</if>
		</where>
		ORDER BY ROOM_ID DESC
	</select>

	<!-- ══════════ 메시지 ══════════ -->

	<select id="selectMessages" resultType="com.surit.chat.dto.ChatMessageDTO">
		SELECT M.MESSAGE_ID,
		       M.ROOM_ID,
		       M.SENDER_NO,
		       M.MESSAGE_TYPE,
		       M.CONTENT,
		       M.FILE_PATH,
		       TO_CHAR(M.SENT_AT, 'YYYY-MM-DD HH24:MI') AS SENT_AT,
		       TO_CHAR(M.READ_AT, 'YYYY-MM-DD HH24:MI') AS READ_AT,
		       U.NAME AS SENDER_NAME
		  FROM CHAT_MESSAGE M
		  JOIN USERS U ON U.USER_NO = M.SENDER_NO
		 WHERE M.ROOM_ID = #{roomId}
		 ORDER BY M.MESSAGE_ID
	</select>

	<!-- MESSAGE_ID 는 IDENTITY 라 넣지 않는다 -->
	<insert id="insertMessage" parameterType="com.surit.chat.dto.ChatMessageDTO">
		INSERT INTO CHAT_MESSAGE (ROOM_ID, SENDER_NO, MESSAGE_TYPE, CONTENT, FILE_PATH, SENT_AT)
		VALUES (#{roomId}, #{senderNo}, #{messageType}, #{content}, #{filePath}, SYSTIMESTAMP)
	</insert>

	<update id="markAsRead">
		UPDATE CHAT_MESSAGE
		   SET READ_AT = SYSTIMESTAMP
		 WHERE ROOM_ID    = #{roomId}
		   AND SENDER_NO != #{readerNo}
		   AND READ_AT IS NULL
	</update>

	<select id="countUnread" resultType="int">
		SELECT COUNT(*)
		  FROM CHAT_MESSAGE
		 WHERE ROOM_ID    = #{roomId}
		   AND SENDER_NO != #{readerNo}
		   AND READ_AT IS NULL
	</select>

	<select id="selectUserName" resultType="string">
		SELECT NAME FROM USERS WHERE USER_NO = #{userNo}
	</select>

</mapper>